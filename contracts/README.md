# Contracts

当前只维护两份对外契约：

- `教师端共享任务表契约.md`：共享 `task_assignments` 的职责、字段权限、状态机、固定任务唯一性和数据库安全边界
- `TIT课程级数据与Mock字段契约.md`：课程级事实、证据状态、来源映射和 Mock 边界

当前任务目录包含连续编号的 `G01`–`G09` 和 5 个个性化改善模板；G04 为“首课准备”，由授课环境拍照 AI 审核和课件准备确认组成。个性化任务、提醒与运营 Case 由已确认的确定性规则产生；Agent 不自由发明任务。历史方案需要追溯时使用 Git，不在当前契约中继续维护。

共享表结构变化只走 Alembic，并与两个后端服务的兼容窗口一起评审。
