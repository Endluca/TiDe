# Build-time safe default. The teacher-web service replaces this file from
# TIDE_TRUSTED_PROXY_CIDRS before Nginx starts.
set_real_ip_from 127.0.0.1/32;
real_ip_header X-Forwarded-For;
real_ip_recursive on;
