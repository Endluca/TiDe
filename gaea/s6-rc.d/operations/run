#!/bin/sh

set -eu
if [ "${TIT_PROCESS_PROFILE:-application}" != "application" ]; then
  exec sleep infinity
fi
cd /app/operations
exec s6-setuidgid gaea env \
  TIT_FRONTEND_REQUIRED=true \
  /opt/venv/bin/python /app/operations/scripts/run_api.py
