# 任务 PRD｜ME Culture & PARSNIP

## 0. 文档信息

| 项目 | 内容 |
| --- | --- |
| 任务 ID / 编码 / 分值 | `me-culture` / `G06` / 4 分 |
| 完成方式 | 阔知多课程视频＋考试 |
| 课程映射 | 课程 `520` 与 `398` |
| 最近更新 | 2026-08-04 |

## 1. 必修映射

- 课程 520：视频 `2791`；考试 `2792`（试卷 `449`）。
- 课程 398：视频 `1948`–`1952`；考试 `1953`–`1957`（试卷 `239`–`243`）。

所有上述任务均为 TIDE 必修。老师可分别免登打开两门课程，完成后在 TIDE 一次刷新全部进度。

## 2. 完成规则

- 所有必修视频 `percent = 100`；
- 每个考试原始得分按配置满分 5 分归一化为百分比，并达到 TIDE 当前发布题库 `me-culture / courses-520-398-2026-07` 的通过线；
- 全部达标后后端才可幂等完成 G06 assignment。

## 3. 临时示例联调

当前开发环境可通过后端环境开关把 G06 进度查询临时映射到示例老师 `360107609`、课程 `131`、视频 `229`、考试 `231`；免登票证始终使用当前认证教师。示例模式必须显示“示例联调”，不得保存快照或更新当前登录老师的 assignment；关闭开关后自动恢复正式映射。
