# 任务 PRD｜ME Culture & PARSNIP

## 0. 文档信息

| 项目 | 内容 |
| --- | --- |
| 任务 ID / 编码 / 分值 | `me-culture` / `G06` / 4 分 |
| 完成方式 | 阔知多课程视频＋考试 |
| 课程映射 | 课程 `520` 与 `398` |
| 最近更新 | 2026-08-04 |

## 1. 必修映射

- 课程 520：视频 `2791`；考试 `2792`（试卷 `449`）。
- 课程 398：视频 `1948`–`1952`；考试 `1953`–`1957`（试卷 `239`–`243`）。

所有上述任务均为 TIDE 必修。老师可分别免登打开两门课程，完成后在 TIDE 一次刷新全部进度。

## 2. 完成规则

- 所有必修视频 `percent = 100`；
- 每个必修考试的阔知 `percent` 均为 100；`score` 仅作为展示与审计证据；
- 全部达标后后端才可幂等完成 G06 assignment。

## 3. 运行边界

G06 只允许读取上述正式课程映射。代码和环境均不提供示例课程开关；当前老师缺少阔知课程数据时显示待同步，不得切换到其他老师或其他课程兜底。
