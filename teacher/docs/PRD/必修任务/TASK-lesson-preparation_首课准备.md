# 任务 PRD｜Lesson Preparation&Device Network Check 首课准备与设备网络检测

## 0. 文档信息

| 项目 | 内容 |
| --- | --- |
| 任务 ID | `lesson-preparation` |
| 固定编码／分值 | `G04` / 3 分 |
| 任务类型 | 固定必修 |
| 模板版本 | V2.2 |
| 文档状态 / 业务口径 | G04 当前页面只保留授课画面检查与底部备课确认；不再提供独立设备网络检测 |
| 负责人 / 协作人 | 嘉荷 / Echo |
| 最近更新 | 2026-08-03 |

## 1. 任务目标与边界

老师在首课前完成两个部分：

1. **授课环境照片检查**：进入任务后可直接打开摄像头，在真实授课位置拍摄一张 16:9 横向照片，逐项检查摄像头角度、光线、背景和着装 4 项标准；
2. **课件准备确认**：在完整拍照与 AI 检测流程的最下方确认已浏览全部课件并完成本节课备课，不要求上传截图，也不得限制打开摄像头、拍照、重拍或提交 AI 检测。

4 项画面检测来自同一张摄像头预览图，不要求分别拍摄。页面不提供上传相册图片作为替代入口。四项标准及示例图片以 Sophia 提供的《Lesson Preparation Checklist》为准；课件准备只做文字确认，不要求截图。

拍照入口使用 Self-intro 标准说明和合格正面参考图。检测未通过时展示具体未通过项和调整建议；参考图只用于拍照前对照，不作为老师需要上传的证明材料。

资源包中课程 400 与 500 的题目属于历史资料，不进入当前 G04 完成步骤。

两个部分在同一个首课准备工作台内完成，不拆成独立 G 任务。旧版 `g02-device-check` 只作为已发布执行版本的兼容步骤保留，不在页面显示，也不参与当前 G04 完成判定。

### 1.1 首课时间与任务内提醒

- 只在首课准备任务详情页内部展示首课开始时间和实时倒计时；
- 临近首课时，倒计时提示使用更明显但紧凑的颜色，不使用全屏弹窗、全局顶部通栏或额外优先级标签；
- 不修改首页、任务列表或其他任务页面；
- 摄像头画面检测通过且课件准备已确认后，任务内部的倒计时提示停止展示；
- Mock 阶段默认把首课设为当前时间后 3 小时，便于直接验收临近首课的提示状态。

## 2. 教师侧流程

```text
查看首课时间与倒计时
  → 打开摄像头并调整当前授课画面
  → 拍摄一张摄像头预览图
  → 同一张图检测 4 项授课准备标准
  → 任一项未通过：自动展开对应示例，按提示调整后重新拍摄
  → 4 项全部通过：保存检测证据并完成画面检查
  → 在完整拍照检测流程最下方确认已浏览全部课件并完成备课
  → 备课确认、照片检查全部通过
  → COMPLETED
```

授课环境照片检测标准：

1. 摄像头角度；
2. 光线；
3. 背景；
4. 着装。

### 2.1 AI 检测口径

- 正式检测统一由后端调用公司 AI 网关，`provider = VOLCENGINE`，当前模型为 `doubao-seed-2-0-lite`；浏览器不得持有或直传网关密钥。
- 规则版本为 `lesson-preparation-camera-view-2026-08-v7-background-veto`，检测项必须且只能包含 `camera_angle / lighting / background / dressing`，不得恢复耳麦、坐姿或其他旧项目。背景为硬性项：必须干净、整洁、安静、稳定并适合正式线上授课；画面中出现其他人员、明显杂物、干扰授课的物体或敏感个人信息时必须重拍。
- `camera_angle` 必须要求老师正脸面对摄像头，双眼、鼻部和嘴部正面清楚可见；侧脸、明显转头、仰头、低头或头部明显侧倾不得通过。脸部中心、头部大小、头顶留白与肩部构图应在小幅自然容差内贴合页面辅助线，摄像头与视线平齐。
- 后端只在提交给 AI 的临时分析副本上叠加与页面一致的白色虚线辅助轮廓，原照片与任务证据不得被覆盖；该轮廓只用于 `camera_angle`，模型检查其他三项时必须忽略。
- 每项必须返回 `PASS / FAIL / UNKNOWN` 和 `0–1` 置信度。只有四项均为 `PASS`，且 `camera_angle` 置信度不低于 `0.90`、其余每项不低于 `0.85` 时，整张照片才可通过；低于对应阈值按 `UNKNOWN` 处理并允许重拍。
- 模型判定后再执行与身份和肤色无关的中央画面亮度硬校验；明显过暗、强眩光或大面积高光溢出不得被模型的名义 `PASS` 放行。
- AI 网关关闭、未配置、超时或响应格式错误属于技术异常，不能判定老师失败或完成任务。
- Sophia 表格中的布置类示例可用于解释单项标准，但只有同时包含合格正面人物画面的照片才能作为整张合格回归样例。

课件准备确认文案：

> I have reviewed all the slides and finished preparing for this lesson.

当前阶段由老师主动勾选确认。后续如果系统能够可靠回传课件浏览和备课行为，可用系统状态替代或补充主动确认，但不得要求老师提交截图。

## 3. 完成判定

- 已确认浏览全部课件并完成本节课备课；
- 已通过浏览器摄像头拍摄当前授课环境；
- 同一张照片的 4 项标准全部通过；
- 页面将任务状态更新为 `COMPLETED`，并保存教师当前进度。

图片检测结果、图片保存策略和回传接口以后端结果为最终事实源。后台可以对通过后的图片进行存储优化，但教师端不把美颜处理作为任务步骤或完成条件。

### 3.1 需要的产品字段

当前资料尚未提供后端真实物理字段 ID，下面仅定义必须表达的业务字段，不作为已存在字段：

| 模块 | 必须记录的字段含义 |
| --- | --- |
| 公共关联 | `teacher_id`、任务实例 ID、任务模板 ID |
| 首课排期 | 首课 ID、首课开始时间、任务内提醒等级和提醒阈值 |
| 授课环境照片检查 | 原始图片 ID、检测状态、4 项检测结果、规则版本、拍摄时间、检测时间、最终图片资源 ID |
| 课件准备确认 | 确认状态、确认时间；未来可扩展课件浏览和备课行为回传 |
| 总任务 | 画面检测状态、课件确认状态、总任务状态、完成时间 |

> 原型中的 `readinessPhoto`、`readinessAnalysis`、`readinessChecks`、`readinessCheckedAt` 等前端状态不是后端正式字段 ID。

## 4. 验收

| 编号 | 验收场景 | 预期结果 |
| --- | --- | --- |
| AC-01 | 从 G04 进入 | 直接展示授课环境照片检查；不要求先完成备课确认或设备网络检测即可打开摄像头 |
| AC-02 | 查看画面检测说明 | 明确说明一张摄像头预览图同时检测 4 项，不要求分别拍摄 |
| AC-03 | 浏览器允许相机 | 显示实时预览，可拍照并生成待检测预览 |
| AC-04 | 相机不可用或未授权 | 显示可理解的权限或设备提示，不提供已有照片上传替代入口 |
| AC-05 | 任一画面标准未通过 | 展示未通过项和调整建议，允许调整后重拍 |
| AC-06 | 4 项画面标准全部通过 | 完成画面检查并保存检测证据，不向老师增加美颜确认步骤 |
| AC-07 | 只完成画面检测 | 总任务保持进行中，并提示完成课件准备确认 |
| AC-08 | 只确认课件准备 | 总任务保持进行中，并提示完成摄像头画面检测 |
| AC-09 | 备课确认与照片检查均完成 | 总任务变为 `COMPLETED` |
| AC-10 | 查看照片操作区 | 不出现独立的摄像头、麦克风或网络检测步骤 |
| AC-11 | 进入首课准备任务 | 不显示“我的首课”独立页签；同一工作台展示首课时间、倒计时和两个操作模块 |
| AC-12 | 距首课 6 小时内且任务未完成 | 只在首课任务内部加强倒计时提示，不出现全局通栏或弹窗 |
| AC-13 | 访问首页、任务列表或其他任务 | 不出现首课提醒新增控件，不改变这些页面的原有结构 |
| AC-14 | 两个部分均完成 | 首课任务内部的倒计时提示停止展示 |
| AC-15 | 尚未打开摄像头 | 拍照入口展示当前业务检测清单中的合格正面示例，不展示旧版参考图 |
| AC-16 | 首次查看画面检测模块 | 示例区默认展开；可在 Camera Angle、Lighting、Background、Dressing 四个分组之间切换，并区分“合格示例 / 需调整示例” |
| AC-17 | 点击任一示例缩略图 | 打开放大查看层，可通过关闭按钮、遮罩或 Esc 退出；不改变任务完成状态 |
| AC-18 | 桌面端查看操作区 | 使用居中的单列流程；摄像头检测是唯一主视觉和主操作，课件确认在全部拍照、检测、重拍提示和隐私说明之后的最下方呈现 |
| AC-19 | 移动端查看操作区 | 保持与桌面端相同的纵向顺序；4 项标准和示例完整可读且不溢出页面 |
| AC-20 | 检测出现未通过项 | 自动展开摄像头模块内的示例区，并默认定位到首个未通过项；老师仍可切换其他分类 |
| AC-21 | 尚未完成备课确认 | 仍可打开摄像头、拍照、重拍并提交 AI 检测；备课确认不参与照片按钮禁用或 AI 判定 |
| AC-22 | 照片已通过后确认备课 | 复用首次合格照片的已通过结果完成总任务，不重复调用 AI，也不覆盖首次合格证据 |
