# 后端开工准备｜阶段五验收报告

> 后续变更：同日项目确认取消世文任务 HTTP 下发／回传，改为共享 PostgreSQL 下的双向只读视图。本文保留首次验收历史；当前实现应以主 PRD V4.14、联动口径 `TIDE-PRD-API-20260721-15`、`0002_shared_database_exchange` 迁移和最新世文集成契约为准。

## 1. 结论

2026-07-21 已完成阶段一至阶段五，并停在业务后端开发前。协作边界、数据库、公共任务合同和世文集成合同均已落盘；本地 PostgreSQL 16 已运行并通过迁移、Seed、关键约束和回滚验证。

本轮验收时尚未建立后端业务源码。目录整理后，`backend/` 只保存数据库、合同和参考资料；仍未实现 NestJS Controller、Service、Worker，也没有连接世文正式接口。

## 2. 分阶段证据

| 阶段 | 证据 | 结果 |
|---|---|---|
| 1. 协作边界 | `COLLABORATION_STANDARD.md` 2.3、2.4；`嘉荷开工检查清单.md` | 嘉荷只依赖公共合同，不能直连数据库或自行改迁移 |
| 2. 数据库设计 | `backend/database/migrations/0001_initial.up.sql`、Down、Seed | 54 张公共表，覆盖账号、任务、文件、AI、投影、集成和审计 |
| 3. 公共任务合同 | `backend/contracts/openapi.yaml`、`task-contract.ts`、3 个 Mock 示例 | 13 条 HTTP 路径，固定和个性化任务共用 `TaskContext` |
| 4. 世文集成合同 | `backend/contracts/世文集成契约.md` | 实时只读、assignment 下发、状态/完成回传三条通道明确 |
| 5. 本地环境 | PostgreSQL 16.14 容器、迁移与验证脚本 | 实际建库、升级、Seed、约束和回滚通过 |

## 3. 本地数据库状态

| 项目 | 实际值 |
|---|---|
| 容器 | `tide-postgres16-dev` |
| 镜像 | `postgres:16.14-alpine` |
| 数据库 | `tide_dev` |
| 本地角色 | `tide_app` |
| 本地端口 | `55432` |
| Schema | `tide` |
| 表数量 | 54 |
| Seed | 1 个固定任务 Mock、1 个个性化任务 Mock、1 个 Mock assignment |
| 现有 PostgreSQL 14 | 仍使用 `5432`，未修改 |

本地 `.env` 已被 Git 忽略；仓库只保存 `.env.example`。数据库卷 `tide_postgres16_data` 保留，停止容器不会删除数据。

## 4. 已执行验证

### 数据库

```bash
bash backend/database/scripts/apply.sh
bash backend/database/scripts/verify.sh
bash backend/database/scripts/rollback-test.sh
```

验证结果：

- PostgreSQL `16.14`；
- `tide` Schema 54 张表；
- 固定任务和个性化任务 Mock 各 1 条；
- 邮箱/教师绑定唯一约束有效；
- `assignment_id` 幂等唯一约束有效；
- Mock 任务生成真实世文事件时被数据库触发器拒绝；
- 临时数据库可完整升级和回滚，回滚后 `tide` Schema 不残留。

### 接口合同

```bash
ruby backend/contracts/verify-contracts.rb
```

验证结果：

- OpenAPI YAML 可解析，共 13 条路径；
- 31 个内部 `$ref` 均可解析；
- 固定、个性化和 assignment Mock JSON 可解析且互相一致；
- TypeScript 合同包含要求的公共接口。

### 文档和仓库

```bash
bash "docs/接口与数据/check_prd_interface_sync.sh"
git diff --check
```

联动口径为 `TIDE-PRD-API-20260721-14`，检查通过；未发现空白错误。

## 5. 后端开工门槛检查

- [x] 数据所有权和嘉荷访问边界无歧义。
- [x] 初始迁移可从空数据库执行。
- [x] 固定任务和个性化任务公共结构已确定。
- [x] OpenAPI、TypeScript 类型和 Mock 示例已提供。
- [x] 世文三条数据通道已定义。
- [x] Mock 不会进入真实世文回传。
- [x] 外部待确认参数已集中记录，不阻塞本地开发。
- [x] PRD 与接口文档联动通过。

## 6. 后续起点

下一阶段才在 `backend/` 初始化业务源码，按账号认证、任务目录/运行、文件、AI、世文适配器和 Outbox 顺序实现。本报告不代表世文正式公网地址、鉴权、只读安全视图或生产联调已经完成。
