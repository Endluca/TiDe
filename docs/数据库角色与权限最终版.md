# 数据库账号与权限最终版

运行权限默认按表级维护，避免为常规 CRUD 建立易漂移的列级授权清单。只有跨服务写所有权、
隐私/密钥、不可逆历史和 DDL 等确有必要的边界才单独收窄；字段不可变、状态机和删除禁令优先由
数据库约束与 Trigger 强制执行。

| 账号 | 权限 | 表 |
|---|---|---|
| `tide_sys_admin` | `SELECT, INSERT, UPDATE, DELETE` | `public.*`、`tide.*` |
| `tit_growth_app` | `SELECT` | `public.teacher_source_wide`、`public.lesson_source_wide` |
| `tit_growth_app` | `SELECT, INSERT, UPDATE, DELETE` | `public.teachers`、`public.complaint_category_rules`、`public.personalized_trigger_matches`、`public.lesson_score_results`、`public.teacher_qualifications`、`public.score_accounts`、`public.score_component_accounts`、`public.score_entries`、`public.task_templates`、`public.task_assignments`、`public.notifications`、`public.ops_cases`、`public.ops_decisions`、`public.outbox_events`、`public.audit_events`、`public.idempotency_records`、`public.config_versions`、`public.config_publication_audits`、`public.operator_accounts`、`public.operator_role_grants`、`public.operator_sessions`、`public.teacher_support_tickets` |
| `tit_growth_app` | `SELECT` | `public.dts_pipeline_control`（运行状态和资格开关只读；切换写入仍禁止） |
| `tit_teacher_crud` | `SELECT, INSERT, UPDATE, DELETE` | `tide.*`，但 `tide.crm_sso_logins` 除外 |
| `tit_teacher_crud` | `SELECT, INSERT, UPDATE` | `tide.crm_sso_logins`（一次性兑换事实禁止删除） |
| `tit_teacher_crud` | `SELECT` | `public.alembic_version`、`public.task_templates`、`public.teachers`、`public.teacher_scorecard_current`、`public.teacher_lesson_score_current`、`public.teacher_g01_status_current` |
| `tit_teacher_crud` | `SELECT, INSERT, UPDATE, DELETE` | `public.task_assignments`、`public.notifications`、`public.notification_events`、`public.teacher_support_tickets` |
| `tit_dts_ingest_runtime` | `SELECT, INSERT, UPDATE, DELETE` | `public.teacher_source_wide`、`public.lesson_source_wide`、`public.dts_ingest_checkpoints`、`public.dts_ingest_events`、`public.dts_source_rows`、`public.dts_dirty_keys` |
| `tide_support_ticket_owner` | `SELECT, INSERT, UPDATE, DELETE` | `public.teacher_support_tickets` |
